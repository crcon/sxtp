/* ===========================================================
   山西二次调频市场 · 政策常量与量化指标体系
   所有指标均从公开文件整理，均可在参数面板中覆写
   -----------------------------------------------------------
   价格单位换算说明：
   · 国家通知：调频里程价格上限 15 元/MW，等价 0.015 元/千瓦
   · 本平台统一使用 元/kWh 作为里程申报价内部单位
     （0.015 元/kWh 对应 15 元/MW 的上限）
   =========================================================== */
const SX_DATA = Object.freeze({
  // ---------- 5 时段划分（山西细则） ----------
  periods: [
    { id:"t1", name:"00:00-06:00", hours:6, cap:[0.005,0.015], type:"后夜",   default_bid:0.008, default_q:0.10 },
    { id:"t2", name:"06:00-12:00", hours:6, cap:[0.005,0.015], type:"早高峰", default_bid:0.010, default_q:0.12 },
    { id:"t3", name:"12:00-16:00", hours:4, cap:[0.010,0.015], type:"中午低谷", default_bid:0.012, default_q:0.13 },
    { id:"t4", name:"16:00-21:00", hours:5, cap:[0.010,0.015], type:"晚高峰", default_bid:0.014, default_q:0.15 },
    { id:"t5", name:"21:00-24:00", hours:3, cap:[0.005,0.015], type:"夜间",   default_bid:0.010, default_q:0.10 },
  ],

  // ---------- 性能折算系数（山西 2025 细则） ----------
  alpha: 0.5,        // 归一化下阈值
  beta:  0.8,        // 结算折算系数
  Kpmin: 1,          // 性能归一化最小值（<=则不予调用）
  Kpsat: 6,          // 性能归一化饱和值
  Kp_cap: 2,         // 国家上限：Kp = K1·K2·K3 ≤ 2

  // ---------- 技术边界 ----------
  minInstructionSec: 30,      // 调频指令最短历时
  ramp_cap_MWmin: 60,         // 独立储能调节速率上限
  precision_pct: 0.01,        // 调节精度：额定功率 1%
  response_time_s: 60,        // 标准响应时间

  // ---------- 需求区间 ----------
  demandRatio: [0.05, 0.15],  // 需求 = 直调发电需求最大值的 5%-15%

  // ---------- 价格上限（国家） ----------
  mileage_price_cap_kwh: 0.015,   // 元/kWh
  mileage_price_cap_MW: 15,        // 元/MW (等价)

  // ---------- 月度调频强度系数（历史经验） ----------
  months: ["1月","2月","3月","4月","5月","6月","7月","8月","9月","10月","11月","12月"],
  monthFactor: [1.02,0.95,0.92,0.90,0.94,1.05,1.12,1.10,0.98,0.92,0.98,1.12],
});

// ---------- 默认参数（含五时段） ----------
const DEFAULT_PARAMS = {
  // 项目基础
  energy:200, duration:2, power:100, battery:"磷酸铁锂", region:"山西省",
  soc_min:10, soc_max:90, eff:0.88, degrade:0.020,

  // 性能系数
  K1:1.65, K2:0.98, K3:0.99,   // K1·K2·K3 = 1.60
  K_max_period:1.80,            // 时段内全市场最大 K实际

  // 5 时段：申报价(元/kWh) & 中标容量比例(0-1)
  bid_t1:0.008, q_t1:0.10,
  bid_t2:0.010, q_t2:0.12,
  bid_t3:0.012, q_t3:0.13,
  bid_t4:0.014, q_t4:0.15,
  bid_t5:0.010, q_t5:0.10,

  // 里程倍数：每 1MW 中标容量·每小时产生的调频里程能量（MW·h 当量）
  // 参考：0.3 ~ 1.2；储能因响应快通常偏高
  mileage_multiplier:0.6,

  // 量价补偿
  p_charge:0.35,        // 调频时段充电电价 元/kWh
  p_discharge:0.40,     // 调频时段放电电价 元/kWh
  cycles_per_day:1.2,   // 调频引起的日等效充放次数
  dod_ratio:0.60,       // 单次调频吞吐占 SOC 窗口比例

  // 容量补偿（扩展口径）
  cap_subsidy:12,       // 元/kW·月
  q_cap_cover:0.80,     // 容量覆盖率

  // 考核
  availability:0.9865,
  qualified_rate:0.9823,
  response_rate:0.9941,
  penalty_rate:0.10,    // 不合格部分扣款比例

  // 成本
  capex_equip:0.90, capex_epc:0.20, opex:18,

  // 财务
  discount:8.00, vat:13, tax:25, life:15, build:6,
};
